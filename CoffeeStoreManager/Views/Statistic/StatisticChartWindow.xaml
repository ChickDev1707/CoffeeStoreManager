<Window x:Class="CoffeeStoreManager.Views.Statistic.StatisticChartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CoffeeStoreManager.Views.Statistic"
        mc:Ignorable="d"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
        WindowStartupLocation="CenterScreen"
        DataContext="{StaticResource StatisticChartVM}"
        Title="StatisticChartWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <ComboBox Grid.Row="0" ItemsSource="{Binding DataCombobox}" 
                  SelectedItem="{Binding ValueOfCbx,
                  UpdateSourceTrigger=PropertyChanged}" Height="19" VerticalAlignment="Top" 
                  />
        
    </Grid>
</Window>
